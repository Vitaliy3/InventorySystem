# Routes Config
#
# This file defines all application routes (Higher priority routes first)
#

module:testrunner
# module:jobs


GET     /                                       App.Index
* /login                                        App.Login
GET /auth                                       App.Auth
GET /logout                                     App.Logout

//                           equipment
GET /getAllEquipments                           Equipment.GetAllEquipments
GET /getFullTree                                Equipment.GetFullTree
GET /getEquipmentsInStore                       Equipment.GetEquipmentsInStore
GET /getEquipmentByUser                         Equipment.GetEquipmentByUser

POST /dragToUser                                Equipment.DragToUser
POST /dragToStore                               Equipment.DragToStore


POST /writeEquipment                            Equipment.WriteEquipment
POST /addEquipment                              Equipment.AddEquipment
POST /updateEquipment                           Equipment.UpdateEquipment

DELETE /deleteEquipment                         Equipment.DeleteEquipment

                              employee

GET /getAllEmployees                            User.GetAllEmployees
POST /addEmployee                               User.AddEmployee
POST /updateEmployee                            User.UpdateEmployee
POST /resetPassEmployee                         User.ResetPassEmployee
DELETE /deleteEmployee                          User.DeleteEmployee

                               InventoryEvents
GET /getAllEvents                               Events.GetAllEvents
GET /getEventsForDate                           Events.GetEventsForDate


# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

# Catch all, this will route any request into the controller path
#
#                    **** WARNING ****
# Enabling this exposes any controller and function to the web.
# ** This is a serious security issue if used online **
#
# For rapid development uncomment the following to add new controller.action endpoints
# without having to add them to the routes table.
# *       /:controller/:action                    :controller.:action
